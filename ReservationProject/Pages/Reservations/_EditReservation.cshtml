@using ReservationProject.Pages.Extensions;

@model ReservationProject.Soft.Pages.Reservations.ReservationsModel

@Html.Editor(model => model.Reservation.ReservationDate)
@Html.Select(model=>model.Reservation.RoomId, Model.Rooms, "Select room")
@Html.Select(model => model.Reservation.WorkerId, Model.Workers, "Select worker")
