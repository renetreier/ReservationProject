@page
@using ReservationProject.Core.Extensions
@model ReservationProject.Pages.ReservationsModel

@{
    Layout = "_Index";
}

<table class="table">
    <thead>
        <tr>
            @Html.TableHeader(model => model.ItemList[0].ReservationDate)
            @Html.TableHeader(model => model.ItemList[0].RoomId)
            @Html.TableHeader(model => model.ItemList[0].WorkerId)
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ItemList)
        {
            <tr>
                @Html.RowData(modelItem => item.ReservationDate)
                @Html.RowData(modelItem => item.ReservedRoom.RoomName)
                @Html.RowData(modelItem => item.ReservedWorker.FullName)
                @Html.RowButtons("Reservations", $"{item.Id}")
            </tr>
        }
    </tbody>
</table>
